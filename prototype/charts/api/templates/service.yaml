apiVersion: v1
kind: Service
metadata:
  name: {{ include "conn3ction-api.fullname" . }}
  labels:
    app: {{ include "conn3ction-api.name" . }}
    chart: {{ include "conn3ction-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "conn3ction-api.name" . }}
    release: {{ .Release.Name }}
