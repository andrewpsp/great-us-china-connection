apiVersion: apps/v1
kind: Deployment
metadata:
  name: conn3ction-api
  labels:
    app: conn3ction-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: conn3ction-api
  template:
    metadata:
      labels:
        app: conn3ction-api
    spec:
      containers:
      - name: api
        image: conn3ction-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        # Uncomment to use etcd
        # - name: ETCD_ENDPOINTS
        #   value: "etcd-0.etcd:2379"
        # - name: ETCD_PREFIX
        #   value: "/conn3ction/records/"
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: conn3ction-api
  labels:
    app: conn3ction-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: conn3ction-api
